# Copy to frontend/.env for local dev. Vite reads VITE_* at dev/build time.

# Backend origin for the dev-server proxy (see vite.config.ts). The app uses
# relative /api paths in the browser; this only sets where /api is proxied to.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
